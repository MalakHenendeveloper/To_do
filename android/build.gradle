
buildscript {
    repositories {
        google() // لإضافة مستودعات Google
        mavenCentral() // مستودعات Maven
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.3.0' // تأكد من أن الإصدار متوافق مع مشروعك
        classpath 'com.google.gms:google-services:4.4.2' // إضافة خدمات Google
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
